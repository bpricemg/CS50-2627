/**
* Initalize multiple variables
*/

#include <stdio.h>

int main(void)
{
    // Intitalize an integer variable x
    int x = 13;

    // Initialize an integer variable y
    int y = 67;

    // Print our the current value of x and y
    printf("The current value of x is %i and the current value of y is %i.\n", x, y);

}
