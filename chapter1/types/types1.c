/**
 * This program will initialize a variable and print.
 */
#include <stdio.h>

int main(void)
{
    // Initialize an integer variable called x with the value 13
    int x = 13;

    // Print out the current value of x
    printf("The value of x is %i\n", x);
}
